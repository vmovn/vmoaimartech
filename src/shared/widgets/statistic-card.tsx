/**
 * StatisticCard is the canonical KPI widget. It re-exports `StatCard` from
 * shared/components so dashboards can import every widget from a single path.
 */
export { StatCard as StatisticCard } from "@/shared/components/stat-card";
