/**
 * Motion design system. Import from a single path:
 *
 *   import {
 *     DURATION, EASE, TRANSITION, variants, gestures,
 *     useReducedMotionSafe, motionProps,
 *     PageTransition, MotionStagger, MotionItem, HoverLift,
 *     SuccessCheck, ErrorCross, TypingIndicator, LivePulse,
 *     Shimmer, CountUp, AnimatedBackdrop,
 *     AnimatePresence, motion,
 *   } from "@/shared/motion";
 */
export * from "./tokens";
export * from "./hooks";
export * from "./components";
