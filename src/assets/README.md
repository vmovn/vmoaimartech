# Assets

- `images/` — raster art (jpg, webp).
- `icons/` — custom svg icons (lucide covers most).
- `illustrations/` — hero and empty-state art.
- `logos/` — brand marks.
- `fonts/` — self-hosted webfonts if any.
